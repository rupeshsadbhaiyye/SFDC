global class newscheduleclass implements schedulable{
    global void execute(system.SchedulableContext sc){
        new_demo_apex_batch nt=new new_demo_apex_batch();
        //id job=database.executeBatch(nt, 10);
        
    }
}